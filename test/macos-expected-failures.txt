# Known macOS test gaps -- burn-down backlog for full macOS parity.
#
# EMPTY, and that is a result rather than an oversight: every entry this file
# was born with reported XPASS(strict) on the macos-15 shards, i.e. the tests
# PASS there. A strict xfail cannot be satisfied by a test that passes, so the
# list burned itself down to nothing the first time the shards ran to completion
# under it. Nothing is marked on macOS today.
#
# Worth recording, because the entries came with a rationale that the run
# falsified: they were grouped under "frame recording is Linux-only BY DESIGN --
# these suites assert the recorder RUNS, so they cannot pass on darwin". They do
# pass on darwin. Whatever the reason (the product moved, or the reading of
# `_frame_record.py`'s gate was wrong), the list is not the place to keep an
# explanation the machine disagrees with, so it is gone with the entries. If a
# genuine intended-platform refusal turns up later, a `skipif` AT THE TEST is the
# right home for it -- a list entry says "this ought to pass and does not", which
# is a different claim.
#
# Applied by conftest.py::pytest_collection_modifyitems (rootdir) on macOS ONLY,
# through the same _apply_tracked_gap_list matcher that applies
# windows-expected-failures.txt: each listed test is marked a STRICT xfail there
# and runs normally on the Linux matrix. Entries are pytest node ids -- file,
# class, function -- and may optionally name ONE parametrization; a bare id
# covers every parametrization of that test. No @group suffix.
#
# Fix the test (or the product gap) and DELETE the line -- additions require a
# reason in the PR. Group entries under a `# reason` heading so the PR body's
# count-by-reason table can be rebuilt from this file.
